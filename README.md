# AI Data Quality Agent — Capstone

## One-line
Automatic multi-agent data quality system: profiling → safe autofix → validation → report + human review UI.

## Quickstart
1. Clone: `git clone https://github.com/kiran465-maker/data-quality-agent-capstone`
2. Create venv:
   ```powershell
   python -m venv venv
   venv\Scripts\activate
   pip install -r requirements.txt
